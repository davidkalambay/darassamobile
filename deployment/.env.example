# Déploiement plateforme e-learning — learn.darassa.academy (VPS, Docker)
# Copier vers .env et renseigner les valeurs (ne pas commiter .env).
# BDD : PostgreSQL (migration future vers AWS RDS prévue).

# --- PostgreSQL (sur le VPS ; plus tard : remplacer par AWS RDS) ---
POSTGRESQL_USER=bn_moodle
POSTGRESQL_PASSWORD=CHANGEZ_MOI_MOT_DE_PASSE_FORT
POSTGRESQL_DATABASE=bitnami_moodle
# Développement uniquement : ALLOW_EMPTY_PASSWORD=yes (déconseillé en prod)
ALLOW_EMPTY_PASSWORD=no

# --- Moodle ---
MOODLE_IMAGE_TAG=5.0

# --- Ports exposés (80/443 pour learn.darassa.academy derrière reverse proxy) ---
HTTP_PORT=80
HTTPS_PORT=443

# --- Domaine public (pour reverse proxy / SSL) ---
# MOODLE_PUBLIC_HOST=learn.darassa.academy

# --- Migration future AWS RDS ---
# Pour basculer sur AWS RDS : créer une instance RDS PostgreSQL, puis dans .env :
# Ne plus lancer le service postgres dans docker-compose (voir README section AWS RDS).
# MOODLE_DATABASE_HOST=<endpoint-rds>.region.rds.amazonaws.com
# MOODLE_DATABASE_PORT_NUMBER=5432
# + identifiants RDS (user, password, database).
